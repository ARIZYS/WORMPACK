
#chunk loader settings
["chunk loading"]
	#Enable verbose logging of placements and removals of chunk loaders
	verboseLoaders = false
	#The radius (in chunks) that the chunk loader will cover
	#Range: > 1
	loadingRadius = 3

